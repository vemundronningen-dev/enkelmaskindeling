# Neon Postgres connection string
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DBNAME?sslmode=require"

# Optional: protects /api/setup (recommended in production)
SETUP_TOKEN="velg-en-hemmelig-token"
